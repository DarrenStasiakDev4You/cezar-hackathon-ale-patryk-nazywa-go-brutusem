# Plan: Resolve PR #55 Merge Conflicts

## Goal

Merge the current `origin/main` into `feat/task-metadata-contract` as requested, resolve all conflicts without dropping either PR #55's task-metadata contract or `main`'s newer task-composer/settings/permissions/notifications work, then push the merge commit to PR #55.

## Current State

- PR head: `9874c269` on `feat/task-metadata-contract`.
- Current base: `origin/main` at `eacb254d`.
- GitHub reports `CONFLICTING` / `DIRTY`.
- Exploration found 10 conflicted files and 23 hunks.
- The primary worktree is dirty with unrelated user changes; all work will happen in a disposable isolated worktree.
- User selected a merge commit rather than a rebase, so no history rewrite or force-push is planned.

## Resolution Steps

1. Fetch `origin/main`, create or reuse an isolated worktree at the PR head, and confirm the worktree contains no unrelated changes.
2. Merge `origin/main` into `feat/task-metadata-contract` and inventory all conflict markers with `git status` and `git diff --cc`.
3. Resolve the shared Extension API files by combining both sides:
   - Preserve `TaskMetadata*`, `TaskMetadata`, and the task-header aliases from PR #55.
   - Preserve `TaskComposer*`, `TaskComposer`, and `main`'s existing settings, permissions, notifications, and other public exports.
   - Keep the barrel exports and contract test assertions complete and type-compatible.
4. Resolve the component-registry source/catalog files by keeping one coherent list of served contracts and core implementations:
   - Keep `TaskHeaderMain`, `TaskComposer`, and `TaskMetadata` where each is served by the merged code.
   - Keep PR #55's metadata core registration and source-list checks alongside `main`'s composer registration.
   - Ensure boundary checks, source lists, core defaults, contract catalogs, and missing-default assertions agree.
5. Resolve `vite.config.ts` and its tests so the eager entry rules include every merged core implementation, including composer and metadata, while retaining the kept-out graph checks.
6. Audit automatically merged task-thread code, especially `run-header.tsx`, `task-header-main.ts`, composer integration, and metadata controller/host boundaries. Verify that metadata remains shell-owned and props-only, while composer behavior from `main` is not regressed.
7. Remove all conflict markers, run formatting/diff checks, commit the conflict resolution as a merge commit, and push normally to `feat/task-metadata-contract`.
8. Re-check GitHub mergeability and PR labels/status. Do not apply `qa-approved`; preserve the existing QA gate and report any remaining CI or browser-test blockers.

## Verification

Run in repository order from the isolated worktree:

1. `npm run typecheck`
2. `npm test`
3. `npm run test:unit`
4. `npm run build`
5. `npm run test:package`

Also run focused extension-api/component-registry/task-thread tests covering the merged contract catalogs, core registrations, source-list/eager-entry guards, metadata host, and task composer. Confirm `git diff --check`, no unresolved markers, the merge commit is pushed, and GitHub no longer reports the PR as conflicting.
